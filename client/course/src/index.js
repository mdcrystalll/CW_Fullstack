import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App'; // Adjust the path accordingly
import reportWebVitals from './reportWebVitals'; // Adjust the path accordingly

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();